export class Customer {
  name: string
  address: string
  creditCard: string

  constructor(name: string, address: string, creditCard: string) {
    this.name = name;
    this.address = address;
    this.creditCard = creditCard;
  }
}
